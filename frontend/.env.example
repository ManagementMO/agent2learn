# Set this to the real HTTPS origin when publishing the website.
SITE_URL=

# Optional approved demo export. An empty value shows a labeled image placeholder.
# Accepts an HTTPS video URL or an origin-relative path, e.g. /demo.mp4.
PUBLIC_DEMO_VIDEO_URL=
