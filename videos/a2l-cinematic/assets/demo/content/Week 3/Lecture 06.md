# Lecture 06 — Feasibility

> Synthetic demo material, written for the Agent2Learn launch film.

## Example 1

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 2

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 3

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 4

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 5

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 6

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 7

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 8

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 9

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 10

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 11

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 12

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 13

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 14

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 15

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 16

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 17

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 18

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 19

Define the decision variables before writing the model.
Record each constraint and its units.
Separate feasibility from the objective.

<!-- demo spacer -->
## Example 20

Define the decision variables before writing the model.
Record each constraint and its units.
## Feasibility

Define the decision variables first.
A feasible solution satisfies every constraint.
The objective ranks feasible solutions.
Check each constraint against your values.

This material is fictional and is not part of any real course.
